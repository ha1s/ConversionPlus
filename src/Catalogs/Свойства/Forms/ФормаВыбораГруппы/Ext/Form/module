////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ - ОБРАБОТЧИКИ СОБЫТИЙ ФОРМЫ

// Процедура - обработчик события "ПередОткрытием" формы.
//
Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	
	Список.Порядок.Установить("Наименование");

КонецПроцедуры // ПередОткрытием()

// Процедура - обработчик события "ПриОткрытии" формы.
//
Процедура ПриОткрытии()
	
	Если НеИзменятьРодителя Тогда
		ЭлементыФормы.Дерево.ТекущийРодитель          = РодительОтбор;
		ЭлементыФормы.Дерево.ИзменятьТекущегоРодителя = Ложь;
		ЭлементыФормы.Дерево.ИзменятьИерархическийПросмотр = Ложь;
		ЭлементыФормы.Дерево.НастройкаОтбора.Найти("Владелец").Доступность = Ложь;
	КонецЕсли;
	
КонецПроцедуры // ПриОткрытии()
