
Процедура СправочникСписокПередНачаломДобавления(Элемент, Отказ, Копирование, Родитель, ЭтоГруппа)
	
	Если ЭтоГруппа Тогда
		Возврат;
	КонецЕсли;
	
	// загружаем описание конфигурации из файла
	Обработка = Обработки.ЗагрузкаСтруктурыМетаданных.Создать();
	ФормаОбработки = Обработка.ПолучитьФорму();
	ФормаОбработки.Открыть();
	
	Отказ = Истина;
	
КонецПроцедуры

Процедура ДействияФормыПоказатьОписаниеКонфигурации(Кнопка)
	
	Если ЭлементыФормы.СправочникСписок.ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
			
	// загружаем описание конфигурации из файла
	ФормаПросмотра = ПолучитьОбщуюФорму("Конфигурация");
	ФормаПросмотра.Конфигурация  = ЭлементыФормы.СправочникСписок.ТекущиеДанные.Ссылка;
	ФормаПросмотра.Открыть();
	
КонецПроцедуры
