
////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ЭЛЕМЕНТОВ ШАПКИ ФОРМЫ

Процедура ОсновныеДействияФормыЗагрузить(Кнопка)
	
	Предупреждение(ЗагрузитьОбработчики(ЭлементыФормы.ТекстМодуля.ПолучитьТекст()), , "Загрузка обработчиков");
	
	Если МассивИменОбработчиков <> Неопределено Тогда
		
		ФормаВладельца = ЭтаФорма.ВладелецФормы;
		ФормаВладельца.ЭтотОбъект.Прочитать();
		ПолучитьАлгоритмыПриОткрытии(ФормаВладельца, ФормаВладельца.ЭтотОбъект, МассивИменОбработчиков);
		ОбновитьПиктограммыАлгоритмов(МассивИменОбработчиков, ФормаВладельца.ЭлементыФормы.ПанельАлгоритмы, ФормаВладельца.ЭлементыФормы);
		
	КонецЕсли;
	
	ЭтаФорма.Закрыть();
	
КонецПроцедуры
